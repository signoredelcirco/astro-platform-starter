---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const navItems = [
  { label: 'Home', href: '/' },
  { label: 'Opinione Non Richiesta', href: '/opinione-non-richiesta' },
  { label: 'A-Normal', href: '/a-normal' },
  { label: 'Chiacchiere', href: '/chiacchiere' },
];
---

<Layout title="Signore del Circo">
  <!-- Navigation -->
  <nav class="sticky top-0 z-50 border-b border-gray-200 bg-white">
    <div class="max-w-6xl mx-auto px-6 py-4">
      <div class="flex items-center justify-between gap-8">
        <div class="font-bold text-lg">Signore del Circo</div>
        <div class="flex gap-2 overflow-x-auto">
          {navItems.map((item) => (
            <a
              href={item.href}
              class="px-3 py-2 rounded-md text-sm font-medium whitespace-nowrap border border-gray-300 hover:bg-gray-50 transition"
            >
              {item.label}
            </a>
          ))}
        </div>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <div class="bg-gradient-to-br from-blue-600 to-purple-700 text-white py-20 px-6">
    <div class="max-w-6xl mx-auto">
      <h1 class="text-5xl font-bold mb-4">Signore del Circo</h1>
      <p class="text-xl opacity-90">Un podcast affascinante su Game of Thrones, intrattenimento e gaming</p>
    </div>
  </div>

  <!-- Latest Episode Section -->
  <div class="max-w-6xl mx-auto px-6 py-16">
    <h2 class="text-3xl font-bold mb-8">Ultimo Episodio</h2>
    
    <div class="bg-gray-50 rounded-lg border border-gray-200 p-8">
      <div class="mb-6">
        <h3 class="text-2xl font-bold mb-3">Game of Thrones: Il Finale Perfetto?</h3>
        <p class="text-gray-600">Episodio #10 • 09/01/2024 • 45 minuti</p>
      </div>
      
      <p class="text-gray-700 mb-6 leading-relaxed">
        Analizziamo il finale di Game of Thrones e discutiamo se era veramente il finale che meritava la saga.
      </p>
      
      <div class="flex gap-4 items-center">
        <button class="px-6 py-3 bg-blue-600 text-white rounded-md font-medium hover:bg-blue-700 transition">
          ▶ Ascolta su Spreaker
        </button>
      </div>
    </div>
  </div>

  <!-- Podcasts Grid -->
  <div class="max-w-6xl mx-auto px-6 py-16 border-t border-gray-200">
    <h2 class="text-3xl font-bold mb-8">I Nostri Podcast</h2>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      {[
        { name: 'Opinione Non Richiesta', episodes: 24, color: 'blue' },
        { name: 'A-Normal', episodes: 18, color: 'green' },
        { name: 'Chiacchiere', episodes: 31, color: 'purple' },
      ].map((podcast) => (
        <a
          href={`/${podcast.name.toLowerCase().replace(/[\s-]/g, '-')}`}
          class={`p-6 rounded-lg border-2 border-gray-200 hover:border-${podcast.color}-600 hover:shadow-lg transition group`}
        >
          <h3 class="text-xl font-bold mb-2 group-hover:text-${podcast.color}-600">{podcast.name}</h3>
          <p class="text-gray-600">{podcast.episodes} episodi</p>
        </a>
      ))}
    </div>
  </div>

  <footer class="border-t border-gray-200 mt-16 py-12 px-6 bg-gray-50">
    <div class="max-w-6xl mx-auto text-center text-gray-600">
      <p>© 2024 Signore del Circo. Tutti i diritti riservati.</p>
    </div>
  </footer>
</Layout>
