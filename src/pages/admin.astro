---
---
<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width" />
  <title>Admin Panel | Signore del Circo</title>
  <script src="https://accounts.google.com/gsi/client" async defer></script>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .login-container {
      background: white;
      border-radius: 12px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.2);
      padding: 3rem 2rem;
      max-width: 400px;
      width: 100%;
    }
    h1 { text-align: center; margin-bottom: 2rem; color: #333; }
    .login-box.hidden { display: none; }
    .dashboard.hidden { display: none; }
    .user-info {
      background: #f5f5f5;
      padding: 1.5rem;
      border-radius: 8px;
      margin-bottom: 2rem;
      text-align: center;
    }
    .user-info img {
      width: 80px;
      height: 80px;
      border-radius: 50%;
    }
    .logout-btn {
      background: #e74c3c;
      color: white;
      border: none;
      padding: 0.75rem 1.5rem;
      border-radius: 6px;
      width: 100%;
      cursor: pointer;
    }
    .logout-btn:hover { background: #c0392b; }
    .google-button {
      display: flex;
      justify-content: center;
      margin: 2rem 0;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h1>Admin Panel</h1>
    <div id="loginBox" class="login-box">
      <p style="text-align: center; color: #666; margin-bottom: 2rem;">
        Accedi con Google per gestire il podcast
      </p>
      <div class="google-button">
        <div id="g_id_onload"
             data-client_id="INSERISCI_IL_TUO_GOOGLE_CLIENT_ID.apps.googleusercontent.com"
             data-callback="handleCredentialResponse">
        </div>
        <div class="g_id_signin" data-type="standard" data-size="large"></div>
      </div>
      <p style="font-size: 0.85rem; color: #999; text-align: center; margin-top: 1rem;">
        <strong>Nota:</strong> Inserisci il tuo Google Client ID nel data-client_id
      </p>
    </div>
    <div id="dashboard" class="dashboard hidden">
      <div class="user-info">
        <img id="userImage" src="" alt="User" style="display:none;">
        <p style="font-size: 1.2rem; font-weight: bold;" id="userName"></p>
        <p id="userEmail"></p>
      </div>
      <div style="background: #f9f9f9; padding: 2rem; border-radius: 8px; margin-bottom: 2rem;">
        <h2 style="color: #333; margin-bottom: 1rem;">Pannello Amministrazione</h2>
        <p>Benvenuto! Potrai presto gestire i tuoi episodi da qui.</p>
        <ul style="list-style: none; margin: 1.5rem 0;">
          <li style="padding: 0.75rem; background: white; margin: 0.5rem 0; border-left: 4px solid #667eea;">Opinione Non Richiesta</li>
          <li style="padding: 0.75rem; background: white; margin: 0.5rem 0; border-left: 4px solid #667eea;">A-Normal</li>
          <li style="padding: 0.75rem; background: white; margin: 0.5rem 0; border-left: 4px solid #667eea;">Chiacchiere</li>
        </ul>
      </div>
      <button class="logout-btn" id="logoutBtn">Esci</button>
    </div>
  </div>
  <script>
    function decodeJwt(token) {
      const parts = token.split('.');
      const base64 = parts[1].replace(/-/g, '+').replace(/_/g, '/');
      return JSON.parse(atob(base64));
    }
    function handleCredentialResponse(response) {
      const decoded = decodeJwt(response.credential);
      localStorage.setItem('adminUser', JSON.stringify({name: decoded.name, email: decoded.email, picture: decoded.picture}));
      showDashboard({name: decoded.name, email: decoded.email, picture: decoded.picture});
    }
    function showDashboard(user) {
      document.getElementById('loginBox').classList.add('hidden');
      document.getElementById('dashboard').classList.remove('hidden');
      document.getElementById('userName').textContent = user.name;
      document.getElementById('userEmail').textContent = user.email;
      if(user.picture) document.getElementById('userImage').src = user.picture;
    }
    window.addEventListener('load', () => {
      const saved = localStorage.getItem('adminUser');
      if(saved) showDashboard(JSON.parse(saved));
    });
    document.getElementById('logoutBtn').addEventListener('click', () => {
      localStorage.removeItem('adminUser');
      location.reload();
    });
  </script>
</body>
</html>
